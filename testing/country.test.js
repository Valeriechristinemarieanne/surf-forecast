import React from "react";
import Country from "../src/country";
import { render, waitForElement } from "@testing-library/react";
import axios from "axios";

jest.mock("axios");

test("Country Component reads url and displays correct country", async () => {});
